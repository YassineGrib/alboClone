# Albo clone — agent instructions

Read `docs/superpowers/STATUS.md` and `docs/superpowers/00-how-we-build.md` before doing anything.

This is a new Flutter + Laravel app. Treat it as architectural Superpowers work.

1. If STATUS is `grill`, use the `grilling` skill. Do not scaffold the app.
2. If STATUS is `spec`, write `docs/superpowers/specs/` for the current slice only.
3. If STATUS is `plan`, use Superpowers `writing-plans`.
4. If STATUS is `execute`, use TDD plus `subagent-driven-development` or `executing-plans`.
5. UI work: `impeccable`. Motion: `emil-design-eng` / `animate`. Flutter structure: `flutter-apply-architecture-best-practices`.

Product notes live in `albo-clone-technical-plan.md`. Settled decisions override that file when they disagree.
